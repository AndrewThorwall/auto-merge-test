name: Enable Auto-Merge
on:
  - push
jobs:
  Enable-Auto-Merge:
    runs-on: ubuntu-latest
    steps:
      - name: Enable auto-merge
        run: gh pr merge --merge --auto ${{ github.ref_name }}
        env:
          GH_TOKEN: ${{ github.token }}
